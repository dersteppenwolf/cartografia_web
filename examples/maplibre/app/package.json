{
  "name": "maplibre-course-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "maplibre-gl": "^6.1.0",
    "vite": "^8.2.0"
  },
  "devDependencies": {
    "@types/geojson": "^7946.0.16",
    "@types/node": "^24.10.11",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  }
}
